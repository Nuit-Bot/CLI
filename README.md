# `@nuit-bot/cli`

A CLI for easy Nuit management

To install:

```bash
bun install -g @nuit-bot/cli
```

## Development setup

1. Clone this repository
2. `bun install` to install dependencies
3. `bun run src/cli.ts` to run the CLI
